---
interface Props {
  heading: string;
  description: string;
}

import PageHead from './PageHead.astro';

const { heading, description } = Astro.props;
---

<PageHead title={heading} />
<p class="single mt-4 text-muted">
  Structural placeholder. This route exists so the rebuild's navigation and
  routing can be verified; its content is filled in by a later task.
</p>
<div class="prose single mt-6"><p>{description}</p></div>
